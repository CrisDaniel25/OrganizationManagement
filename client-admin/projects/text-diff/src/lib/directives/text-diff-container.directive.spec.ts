import { TextDiffContainerDirective } from './text-diff-container.directive';

describe('TextDiffContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new TextDiffContainerDirective();
    expect(directive).toBeTruthy();
  });
});
